     <nav class="navbar navbar-icon-top navbar-expand-lg navbar-dark bg-dark navbar-top fixed-top">
  <a class="navbar-brand" href="/dashboard"><img src="/assets/logoo.png" width="150" height="70" alt=""></a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto">
      <li class="nav-item active">
        <a class="nav-link" href="/deals/deals_products">
          <%if current_user.has_role? :admin%>
          Create  Deal
          <%end%>
          <span class="sr-only">(current)</span>
          </a>
      </li>
    </ul>
  </div>
</nav>
      <div class="container">   
          <h2 align="center">Todays deals:</h2>  
            <table class="table table-hover">
              <thead class="thead" align="center">
                <tr>
                  <th>Name</th>
                  <th>Description</th>
                  <th>Discount Price</th>
                  <%if current_user.has_role? :admin %>
                  <th>Delete</th>
                  <%end%>
                </tr>

              </thead>
            
              <tbody>
                <% @deals.each do |deal|%>
                <tr align="center">         
                  <td><%= deal.title %></td>
                  <td><%= deal.description%></td>
                  <td><%= deal.discount_price%></td>
                  <%if current_user.has_role? :admin%>
                  <td> <%= link_to '', deal_path(deal),method: :delete,data: { confirm: 'Are you sure?' },class: "fas fa-trash-alt "%></td>
                  <%end%>
                </tr>
                <%end%>
              </tbody>
            </table>
          </div>